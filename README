npm init

npm install nodemon jest supertest --save-dev

npm install body-parser cors dotenv express consign

Configurar packe.js

"main": "server.js",

"scripts": {
    "start": "nodemon server.js",
    "test": "jest --detectOpenHandles",
    "lint": "eslint src/** test/** --fix",
    "secure-mode": "jest --watchAll --verbose=true"
  },
